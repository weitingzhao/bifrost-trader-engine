# 监控页面 UI 修改规则与参考资源

## 规则：监控页面修改原则

- **技术栈**：保持现有 React SPA + 现有 API，不迁移到其他模板技术栈（如 Node+EJS）。
- **Skote 模板**：仅作**视觉与布局参考**，不照搬多页面/侧栏结构；可选借鉴卡片阴影与圆角、表格样式、仪表盘小部件排版、按钮/标签细节。
- **需求优先**：监控页面向局域网内操作者提供红绿灯、自检、状态摘要、操作列表与控制（R-M5/R-M3）；功能满足前提下再做视觉微调。
- **布局约定**：守护程序、对冲程序、近期操作为**功能向**，集中在一块显示（监控与控制）；**IB 账户**为业务向，单独分区。

## 参考资源：Skote 模板路径

- **本机路径**（用于今后做页面 UI 时借鉴）：
  - `~/Desktop/framework/Skote_Nodejs_v4.2.0`
  - 或绝对路径：`/Users/vision-mac-trader/Desktop/framework/Skote_Nodejs_v4.2.0`
- **说明**：Skote 为 Admin/Dashboard 模板（Express + EJS + Bootstrap 5），可参考其 `Admin/views/` 下 dashboard、crypto、tables 等页面的卡片、表格、网格布局与视觉细节；不引入其代码或依赖。

## 何时更新本规则

- 若产品需求或运行环境变更导致监控页面职责变化，可相应调整「布局约定」。
- 若 Skote 模板路径变更（如换机器或目录），更新「参考资源」中的路径。

'use client'

import { TradingLayout } from '@/layouts/AppLayout'

export default function TradingRouteGroupLayout({ children }: { children: React.ReactNode }) {
  return <TradingLayout>{children}</TradingLayout>
}

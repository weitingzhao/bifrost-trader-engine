/**
 * Strategy Instance detail sidebar width — used by Instances list (`DetailSidebar`)
 * and any embedded inspector that shows `StrategyInstanceDetailPage` (e.g. Positions).
 *
 * Keep in sync with `--instance-detail-sidebar-width` in `App.css` (:root).
 */
export const INSTANCE_DETAIL_SIDEBAR_WIDTH_PX = 960
